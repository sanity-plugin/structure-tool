import { authorType } from '@/schemas/documents/authorType';
import { settingType } from '@/schemas/documents/settingType';

const schemaDocuments = [authorType, settingType];

export default schemaDocuments;
