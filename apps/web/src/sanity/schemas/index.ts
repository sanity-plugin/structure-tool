import schemaDocuments from '@/sanity/schemas/documents';
import schemaObjects from '@/sanity/schemas/objects';

const schemaTypes = [...schemaDocuments, ...schemaObjects];

export default schemaTypes;
