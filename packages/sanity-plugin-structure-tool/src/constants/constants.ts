/**
 * Package-wide internal constants for ID generation and singleton keys.
 */
export const constants = {
  SINGLETON_KEY: 'singleton',
  URL_PATH_SEPARATOR: '.',
  UNIQUE_ID_FIRST_VALUE: '1',
} as const;
